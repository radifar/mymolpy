name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

  # Package dependencies
  - numpy
  - matplotlib

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

